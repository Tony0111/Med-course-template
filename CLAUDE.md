# CLAUDE.md

本文件夹是跨学科课程学习模板。AI 工作规范的唯一正文是 **`AGENTS.md`**，开工前先完整读取它。

三条不可违反的底线（细节见 AGENTS.md）：

1. `raw/` 是原件区，只读：不修改、不移动、不改名。
2. 引用必须能回到真实文件与位置（PDF 页 / 印刷书页、幻灯片页、音频时间范围）。
3. 不预建空分类、模板和索引；先有实际材料和问题，再决定是否新增页面。

另外两条：

4. `知识点.md` 是知识点编号（`K01`…）的唯一来源，其他文件只引用编号、不重写名称。
5. skill 只产出「候选」；`已核对` 必须写明依据，脚本命中不算核对。
