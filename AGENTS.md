# AGENTS.md — AI 工作规范

> 本文件是 AI 工作规范的唯一正文。`CLAUDE.md` 只指向本文件，不维护第二套规则。
> 本文件夹是**跨学科课程学习模板**：适用范围不限于医学。复制为某门课的文件夹后，把下面「课程信息」换成真实内容，其余结构按需保留。

## 0. 课程信息（复制后替换）

- 课程名称：`<待填>`
- 学期 / 起止时间：`<待填>`
- 教材（名称、版次）：`<待填>`
- 任课教师：`<待填>`
- 材料编号规则：`L01`、`L02` …（库内稳定编号，**不自动等于**正式第一课、第二课；日期与正式课次需另行确认）

未确认的信息明确留空，不根据导入时间或文件顺序猜测。

## 1. 进入与保护

1. 日常入口是 `index.md`（找教材、找课堂页、找已有笔记）。
2. 按章节回查教材用 `教材导航.md`（章节 → 印刷书页 → PDF 文件 → PDF 页）。
3. 按知识点回查用 `知识点.md`（`K` 编号 → 章节、印刷页、PDF 页），它是编号的唯一来源。
4. PPT 定位用 `PPT导航/<课件名>.md`（幻灯片 → K 编号）。
5. 课堂材料看 `lessons/Lxx.md`（原始材料入口、主题对齐表、疑问与纠错）。
6. `raw/` 是原件区，**只读**：不修改、不移动、不改名、不覆盖原始文件。
7. `raw/md/` 与 `lessons/Lxx-转写.md` 是明确标注的**可重建阅读副本**，不是新的独立来源，也不在这里维护个人总结。
8. 自动提取文本与其原件属于同一来源，**不能当成两份独立证据**。

```
课程文件夹/
├── index.md                日常首页
├── 教材导航.md             教材目录与原件页码入口
├── 知识点.md               知识点编号 K01… 的唯一来源
├── PPT导航/<课件名>.md      幻灯片 → K 编号
├── raw/                    原件区（只读）
│   ├── PDF/                教材 PDF（原件）
│   ├── md/                 可选教材转换副本（保留配图与页码映射）
│   ├── 目录/               目录照片，有需要才保留
│   ├── PPT/                教师发放的课件原件（pptx/pdf，保留版本区别）
│   └── recordings/L01/     原始音频 + 原始字幕
├── lessons/                L01.md（课堂页）、L01-转写.md（自动生成）
├── wiki/                   按需建立的长期知识笔记，暂不分类
├── scripts/                辅助工具 + 测试（清单见 scripts/README.md）
├── .claude/skills/         三个流程 skill（教材/PPT/对齐）
├── .pi/settings.json       让 pi 也读取 .claude/skills
├── AGENTS.md               本文件（AI 工作规范唯一正文）
├── CLAUDE.md               使用对应工具时的简短入口
├── log.md                  简短变更记录，不是学习入口
└── 医学课程学习框架说明.md  来源参考文档，不参与检索
```

## 2. 工作流

### 2.1 按材料类型的处理链

| 触发 | 做什么 | 产出 / 更新 |
|---|---|---|
| 放入教材 PDF | 登记文件名、覆盖范围、缺口；核对版次与页码后才写页码 | `教材导航.md`、`index.md` 材料状态、`log.md` |
| 有了可用的 MD 转换 | 登记「原件 ↔ 转换副本」并抽查连续正文、章节标题、带批注页、表格与图注 | `教材导航.md`「可选阅读副本」、`log.md` |
| 要按知识点学/教某一章 | skill `textbook-knowledge-map` | `知识点.md`（新 K 编号，状态 `候选`）、`教材导航.md` 的 K 范围、`log.md` |
| 收到 PPT | skill `ppt-knowledge-map` | `PPT导航/<课件名>.md`；必要时向 `知识点.md` 追加候选条目；`index.md` 的 PPT 列、`log.md` |
| 收到录音 + SRT | 运行 `scripts/import_srt.py` 生成转写页；首次使用该课录音时抽查音文对齐 | `lessons/Lxx-转写.md`、`log.md` |
| 要把录音/字幕对到知识点 | skill `lesson-align` | `lessons/Lxx.md` 的主题对齐表与疑问记录；`index.md` 课堂表、`log.md` |
| 收到新版课件或补充材料 | 在原课堂页/导航文件里更新引用与版本说明，标明旧页码不再有效 | 对应页面、`log.md` |
| 有问题要查 | 先定位到教材页 / 幻灯片页 / 录音时间，再解释 | 默认留在对话；只有值得复用的才写页面 |
| 出现值得反复用的理解 | 建或更新 `wiki/<标题>.md` | `index.md`「已有笔记」、`log.md` |

顺序约束：`知识点.md` 先有（至少本章）→ PPT 导航 → 录音对齐。缺前置就先建前置，不跳过直接猜编号。

### 2.2 一次课的时间线

| 阶段 | 做什么 | 不做什么 |
|---|---|---|
| 课前 | 从 `教材导航.md` / `知识点.md` 进入，读原文与图表，带少量问题预习 | 不等转换完成，不要求先生成整章摘要 |
| 课后 | 音频 + SRT 配对放入 `raw/recordings/Lxx/`；生成转写页；补主题定位与疑问 | 不为「整理完成」硬填表格 |
| PPT 到达 | 补进同一课堂页与 `PPT导航/`，标明版本 | 不另建一套课件摘要 |
| 答疑 | 先定位教材 / PPT / 字幕片段，再解释，答案附可回查位置 | 不把资料未覆盖的内容说成教材结论 |
| 复习 | 先自己解释或作答，再根据实际回答纠正；问题回到相关页面修 | 不建掌握度数据库，不批量生成待做题目 |

### 2.3 状态词表（全库统一）

| 状态 | 含义 | 谁能写 |
|---|---|---|
| `候选` / `候选对应` | 由目录、关键词或脚本线索得到的**可能**位置 | AI 可以写 |
| `已核对` | 已回看原件页面或回听录音确认，并写明依据 | 只有核对之后 |
| `未命中` | 按名称与别名检索后确实没找到，如实保留 | AI 可以写 |
| `待核对` | 已登记但尚未验证 | AI 可以写 |
| `待补` | 已知存在但材料缺失（缺页、未发课件等） | AI 可以写 |
| `未收到` | 原件尚未进入 `raw/` | AI 可以写 |

**脚本命中、关键词匹配、AI 推断都不能升为「已核对」。**

### 2.4 复制本模板开始一门新课程

1. 复制整个文件夹并改名为课程名；两门课的材料、日志与已确认状态不混在一起。
2. 替换 `AGENTS.md` 第 0 节的课程信息；清掉 `index.md`、`log.md` 里的示例行与历史记录（保留结构）。
3. 放入实际已有的教材 → 建 `教材导航.md` 与 `index.md` 材料状态；有低成本、质量可用的 MD 转换再放入 `raw/md/` 并登记、抽查。
4. 录音、PPT 到达后按 2.1 逐项补；`L01`… 编号分配后不重排、不回收。
5. `.claude/skills/`（三个流程 skill）与 `.pi/settings.json` 随文件夹复制即可继续用；`scripts/` 保留依赖说明与测试，在新环境跑一次 `unittest`。
6. 检查：能否从 `index.md` 走到已有材料？页码与时间是否可回查？未知状态是否如实保留？

### 2.5 参考文档

`医学课程学习框架说明.md` 是本模板的来源说明（人体寄生虫学整理经验），供人阅读参考，**不参与课程检索**，也不是需要长期维护的文档。它与本文件冲突时，以本文件为准。

## 3. 引用与不确定性

1. 每个重要结论都要能回到**真实文件 + 位置**：PDF 页 / 印刷书页、PPT 文件与幻灯片页、音频时间范围、字幕块。
2. **印刷书页 ≠ PDF 页码**。扫描件可能含封面、前言、空白页或缺页；偏移量只有核对后才能使用，不能假设全书固定相差同一数值。
3. 原始资料、自动转写、个人理解、AI 推断必须分开表述。课堂原话、考核说明、模型建议分别记录，不把 AI 常识写成「教师强调」，也不根据重复次数宣布「必考」。
4. 数字、单位、否定词、专有名词、专业术语是优先核对对象。转写或提取结果读起来顺，不等于正确。
5. 材料之间表述不同时，保留各自出处与版本，写明尚未解决的差异，不强行合成一条「看起来一致」的结论。
6. 图形、结构、流程、表格中的限定条件，文字检索找不到就回看原页或原图；读到图片路径不等于看过图片。
7. 没有提供的页面只登记缺口，**不生成虚假跳转链接**。

## 4. 什么时候建页面 / 笔记

1. 先有实际问题，再决定是否记录；先有实际材料，再建立对应记录。
2. 能在现有页面解决的问题，就不新增文件。没有内容，就不预建分类、模板和索引。
3. 新增前先问：它具体减少了哪一步查找、重复记录或复习的成本？说不清就暂时不建。
4. 一节课的小疑问先留在课堂页；跨课反复用到的机制联系、易混概念比较、鉴别思路、核对后的解释，才提炼为 `wiki/` 知识笔记。
5. `wiki/` 不预设子目录，用具体标题和链接组织；只有实际查找困难时才调整。
6. 错题留在相关笔记里（真实原答案、错误原因、依据、复测结果）；没有实际作答就不生成「我的错题」，不建立错题管理目录或掌握度数据库。
7. 原转写里的专业词疑点在课堂页另记「原词、候选正确词、依据、核对状态」；**不把 AI 猜测覆盖进原文**，也不把转写错误直接认定为教师说错。
8. 普通问答默认留在对话中；只有需要再次使用的解释、比较或纠错，才更新课堂页或知识笔记。

## 5. 知识点主干与三个 skill

知识点必须有稳定编号，否则三份导航会各写一套名称，无法对账。`知识点.md` 是编号的唯一来源，其他文件只引用 `K` 编号：

| 文件 | 维护什么 | 维护者 |
|---|---|---|
| `知识点.md` | K 编号、名称、章节、印刷页、PDF 页、状态 | `textbook-knowledge-map`、`ppt-knowledge-map` |
| `教材导航.md` | 章节 → 文件/页码/缺口 + 知识点范围 | `textbook-knowledge-map` + 人工 |
| `PPT导航/<课件名>.md` | 幻灯片 → K 编号（源侧映射，跨课复用） | `ppt-knowledge-map` |
| `lessons/Lxx.md` | K 编号 → 教材页 / PPT 页 / 录音时间 / 核对状态 | `lesson-align` |

规矩：

1. 一个映射只在它那一侧写一次；跨源查询（「K07 都在哪出现」）当场用搜索或脚本汇总，不做手工汇总表。
2. 三个 skill 默认只产出 `候选对应`；`已核对` 必须写明依据（回看的页 / 回听的时间与原话）。**脚本命中不算核对。**
3. 教材知识点按章增量，不做整本生成；一份 PPT 一个导航文件；一次课一行一次对齐。
4. 规则只存在于 `.claude/skills/`（`textbook-knowledge-map`、`ppt-knowledge-map`、`lesson-align`），`.pi/settings.json` 把该目录接给 pi，不复制第二份。

## 6. 页面格式（按需使用，栏目不必齐全）

### 6.1 课堂页 `lessons/Lxx.md`

```markdown
# L01 课堂材料标题

## 原始材料

- 授课日期、教师：确认后填写。
- 教材、PPT、音频、SRT：列出实际来源；未收到的标明状态。
- 转写：存在阅读副本时添加入口。

## 主题对齐

| 主题 | 教材位置 | PPT 位置 | 录音位置 | 核对状态 |
|---|---|---|---|---|

## 疑问与纠错

仅记录实际遇到的问题、原表述、核对依据和当前结论。

## 录音核对

有检查时简记时间轴结果、抽样范围和未核对的部分。
```

- 一行围绕一个**有用的主题或片段**，不是每条字幕建一行，也不是每个术语疑点都建页。
- 「候选对应」＝ 依目录、关键词或上下文找到的可能位置；「已核对」要说明核对了什么。音频与字幕对应，不自动证明教材与课堂内容一致。
- 这张表是完整对应关系的**唯一**维护位置；首页只链接课堂页，知识笔记只引用相关片段。
- 没有疑问、补充或检查结果的部分直接省略。

### 6.2 转写页 `lessons/Lxx-转写.md`

由 `scripts/import_srt.py` 生成的可重建阅读副本，**不手工修改**。保留字幕文字、说话人标签、编号、起止时间与来源哈希。需要更新 SRT 或生成方式时，先保存必要旧版本，再重建并复核原有段落引用。

### 6.3 知识笔记 `wiki/<中文标题>.md`

中文命名（如 `血压.md`）。先写它解决的问题，再写必要解释或比较、可回查来源、尚未解决的边界；不要求各篇栏目一致，不把整段聊天记录原样搬入。

```yaml
---
type: note
title: <标题>
created: <YYYY-MM-DD>
updated: <YYYY-MM-DD>
sources: <引用来源数量>
related:
- "[[相关笔记]]"
---
```

### 6.4 首页 `index.md` 与教材导航 `教材导航.md`

- 首页只回答三件事：教材从哪进、有哪些课堂材料、已有哪些值得反复看的笔记。不放整课摘要，不复制教材目录全文。
- 教材导航以教材实际目录为依据，记录章节、印刷书页、PDF 文件与文件页码；记录缺页、未提供附录、目录与正文标题差异等材料问题。导航细化到足以定位即可，不为每个段落建条目。

### 6.5 `知识点.md` 与 `PPT导航/<课件名>.md`

这两个文件的表格格式只写在对应 skill 里，不在此重复：

- `知识点.md` → `.claude/skills/textbook-knowledge-map/SKILL.md`
- `PPT导航/<课件名>.md` → `.claude/skills/ppt-knowledge-map/SKILL.md`

不变的两条：只引用 `K` 编号不重写名称；状态只能是 2.3 节词表里的值。

## 7. 辅助设施与命令

- `log.md`：简短记录材料导入、结构变化、重要笔记变更；不保存每次问答，不复制课程摘要。
- `scripts/`：只做重复、可验证的操作，不替 AI 决定哪些内容是知识结论或重点。保留 `requirements.txt`、测试与简短说明；解释器路径按新环境设置，不依赖固定盘符。
- 使用 Claude 等工具时，规则只写在 `AGENTS.md`，`CLAUDE.md` 仅作入口，不重复散布规则文件。
- `.obsidian/`（软件设置）与 `.claude/`（工具配置）不属于课程知识结构；迁移时不盲目复制整套配置、旧工作区、缓存和技能目录。
- 恢复备份放在学习库之外；临时 ZIP 不作长期备份。

```powershell
# 示意参数结构，执行前替换为已有原文件路径
python -B -X utf8 scripts/import_srt.py --lesson L01 --srt "raw/recordings/L01/原始字幕.srt" --audio "raw/recordings/L01/原始音频.m4a"
python -B -X utf8 scripts/extract_slides.py "raw/PPT/L02-心脏.pptx" --list
python -B -X utf8 scripts/find_terms.py --lesson L01 --terms "房室结,房室束" --merge-gap 120
python -B -X utf8 -m unittest discover -s scripts -p "test_*.py"
```

`import_srt.py` 只检查格式、连续编号、有效时间范围与重叠，并拒绝覆盖内容不同的已有阅读页；`extract_slides.py` 只提取 `.pptx` 文字与备注（标准库实现，不依赖 python-pptx），不含图片与版式；`find_terms.py` 只给候选时间段与字幕段链接，不判断讲解内容。
三者**都**不检查音频总时长、不识别音频内容、不完成教材或 PPT 对齐。新课程必须独立核对，不能沿用其他课程的检查结论。

## 8. 完成后检查

1. 首页是否仍只承担入口，而不是越来越长的资料汇总？
2. 同一份对齐关系是否只在一个课堂页维护？
3. 新增笔记是否解决了真实且值得复用的问题？
4. 是否出现了没有内容的分类、重复说明或只为流程存在的管理页面？
5. 原件、自动生成内容与个人理解是否仍然分得清？
6. 每个重要引用是否能定位到真实文件、页面或时间范围？
7. 未知状态是否被如实保留，而不是被猜出或补全？

## 9. 学科边界

课程材料不能自动等同于当前实务建议。涉及实际诊疗、用药剂量、法律意见、工程规范等专业实践时，应另行核对适用情境与可靠的现行依据。涉及可识别个人信息的材料（病例、同学发言等），仅在获准范围内使用，不因为接入 AI 就默认允许上传到外部服务。
